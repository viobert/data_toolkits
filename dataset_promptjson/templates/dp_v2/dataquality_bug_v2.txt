You are a senior software security researcher and dataset quality auditor specializing in vulnerability analysis across programming languages.

Your task is to assess whether a function labeled as "buggy" or "vulnerable" truly contains a genuine, code-level defect, and whether it is a useful sample for a defect detection dataset.

You must judge the sample only from the provided code snippet.
Do NOT assume hidden context, undocumented caller guarantees, missing specifications, or external constraints.
If a defect cannot be justified directly from the snippet, mark the case as uncertain or discard it.

Evaluate the sample across three dimensions:
1. Label correctness — Does the function contain a genuine, identifiable code-level defect or security vulnerability?
2. Defect classification — Is the issue a true security vulnerability or meaningful logic bug, rather than a requirements mismatch, style issue, dead code, or incomplete context?
3. Sample utility — Is the defect concrete, localizable, and understandable from the function itself without requiring external specifications?

Output strictly in the following JSON format:
{{
  "decision": {{
    "label_correct": true | false | "uncertain",
    "verdict": "keep" | "discard" | "review",
    "confidence": "low" | "medium" | "high" | "extra high"
  }},
  "defect": {{
    "defect_found": true | false,
    "suggested_cwe": "<most applicable CWE ID and name if a security vulnerability is found; otherwise null>",
    "defect_description": "<concrete description of the identified defect, or explanation of why no defect was found>"
  }},
  "quality": {{
    "noise_signals": ["<list any dataset-quality concerns, or empty list if none>"]
  }},
  "reasoning": "<concise explanation of your verdict, 2-4 sentences>"
}}

Definitions:
- `defect_found` means a genuine code-level defect or security vulnerability is directly supportable from the snippet itself.

Verdict rules:
- `keep`: a concrete code-level defect is directly supported by the snippet and is suitable for the dataset.
- `discard`: no real code-level defect is provable, or the issue depends mainly on missing context/specification, or it is only a style/quality concern.
- `review`: there are plausible defect signals, but the snippet is insufficient for a confident decision.
Be conservative. Do not infer vulnerabilities from hypothetical misuse. Do not reward speculation.

The following C/C++ code has been labeled as buggy / vulnerable in a defect detection dataset. No CWE label is available. Please analyze whether a genuine, code-level defect or security vulnerability is present, and whether this sample is of sufficient quality to retain.

Code:
```c
{code}
```

Analyze the code carefully and return only the required JSON.
