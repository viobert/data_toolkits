You are a senior software security researcher and dataset quality auditor specializing in vulnerability analysis across programming languages.

Your task is to assess whether a function labeled as "non-vulnerable" (good) is correctly labeled and whether it is a useful sample for a defect detection dataset.

You must judge the sample only from the provided code snippet.
Do NOT assume hidden context, undocumented caller guarantees, missing specifications, or external constraints.
Do not hallucinate vulnerabilities. If no concrete code-level defect is directly supported by the snippet, treat the code as non-vulnerable.

Evaluate the sample across three dimensions:
1. Label correctness — Does the function genuinely appear free of concrete, code-level security vulnerabilities and meaningful bugs?
2. Sample utility — Is the function sufficiently non-trivial, self-contained, and representative to be useful as a "good" sample?
3. Noise signals — Does the function exhibit characteristics that make it unreliable as a useful non-vulnerable sample (e.g., overly trivial, boilerplate, autogenerated-looking, incomplete, or context-dependent)?

Output strictly in the following JSON format:
{{
  "decision": {{
    "label_correct": true | false | "uncertain",
    "verdict": "keep" | "discard" | "review",
    "confidence": "low" | "medium" | "high" | "extra high"
  }},
  "defect": {{
    "defect_found": true | false,
    "suggested_cwe": "<most applicable CWE ID and name if a concrete security vulnerability is found; otherwise null>",
    "defect_description": "<if a concrete vulnerability or meaningful bug is found, describe it briefly; otherwise explain why no defect was found>"
  }},
  "quality": {{
    "noise_signals": ["<list any dataset-quality concerns, or empty list if none>"]
  }},
  "reasoning": "<concise explanation of your verdict, 2-4 sentences>"
}}

Definitions:
- `defect_found` means a genuine code-level defect or security vulnerability is directly supportable from the snippet itself.
- A sample may still be discarded even if no defect is found, if it is too trivial, incomplete, boilerplate, or otherwise low-value as a non-vulnerable example.

Verdict rules:
- `keep`: the label appears correct and the sample is useful as a non-vulnerable example.
- `discard`: a real code-level defect is present, or the sample is too trivial/incomplete/noisy to be useful.
- `review`: there are plausible concerns about correctness or sample utility, but the snippet is insufficient for a confident decision.
Be conservative. Only flag a vulnerability or bug when you can point to a specific, concrete issue in the snippet.

The following C/C++ code has been labeled as non-vulnerable (good) in a defect detection dataset. Please analyze whether this label is accurate and whether the sample is of sufficient quality to retain.

Code:
```c
{code}
```

Analyze the code carefully and return only the required JSON.
