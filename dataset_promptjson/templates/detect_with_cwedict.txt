You are a security-focused code reviewer. Your task is to analyze the provided code snippet for potential vulnerabilities by referencing the given CWE (Common Weakness Enumeration) dictionary.

## CWE Reference Dictionary

The following CWE entries define the vulnerability types you should check against:

[CWE-DICT]

---

## Code Under Review

```
{code}
```

---

## Instructions

1. Carefully read the code and identify any patterns or constructs that match one or more entries in the CWE dictionary above.
2. Determine whether the code contains a vulnerability.

## Output Format

**If a vulnerability IS found**, respond strictly in the following JSON format:

```json
{{
  "has_error": true,
  "cwe_id": "<a list of the matched CWE ID from the dictionary>", e.g. ["CWE-234"] e.g. ["CWE-123", "CWE-79"].
  "issue": "<a detailed description covering: (1) which specific line(s) or construct in the code introduce the vulnerability; (2) the root cause — why this pattern is dangerous; (3) what an attacker could do to exploit it; (4) what the correct fix or safe alternative should be>"
}}
```

**If NO vulnerability is found**, respond strictly in the following JSON format:

```json
{{
  "has_error": false,
  "cwe_id": null,
  "issue": null
}}
```

## Rules

- Only report vulnerabilities that are directly evidenced in the provided code.
- Do not speculate about hypothetical future changes or missing context outside the snippet.
- Match against the CWE dictionary entries only; do not introduce external CWE categories.
- Keep the `issue` field detailed: identify the vulnerable construct, explain the root cause, describe the potential exploit impact, and suggest the correct fix.
- Return valid JSON only — no extra explanation outside the JSON block.
